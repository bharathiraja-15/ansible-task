content = <<EOF
[frontend]
frontend ansible_host=${aws_instance.frontend.public_ip} ansible_user=ec2-user ansible_ssh_private_key_file=./firstserver.pem

[backend]
backend ansible_host=${aws_instance.backend.public_ip} ansible_user=ec2-user ansible_ssh_private_key_file=./firstserver.pem
EOF
